# Design

Reserved for future static UI prototypes. Production UI lives in [frontend/](../frontend/README.md).

Previous `concept-v2` HTML prototypes were ported into the React app (`frontend/src/disks/`, `*-v2.css` styles) and removed from this folder.
