/** Compatibility shim — implementation: it-services/network-privateendpoint/utils/privateEndpointNormalize */
export {
  formatPrivateEndpointConnection,
  enrichPrivateEndpointRow,
  privateEndpointDisplayConnection,
} from '../it-services/network-privateendpoint/utils/privateEndpointNormalize';
