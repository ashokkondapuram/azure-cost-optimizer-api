apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: costoptimize
  labels:
    app: postgres
    app.kubernetes.io/part-of: costoptimize-recommender
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  clusterIP: None
