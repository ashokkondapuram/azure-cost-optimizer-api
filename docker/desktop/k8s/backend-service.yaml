apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: costoptimize
  labels:
    app: backend
    app.kubernetes.io/part-of: costoptimize-recommender
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
