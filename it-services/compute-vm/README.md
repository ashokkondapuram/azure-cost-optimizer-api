# Virtual machine (`compute-vm`)

IT service folder for **compute/vm**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/compute-vm/manifest.yaml` |
| Backend | `it_services/compute_vm/` |
| Frontend | `frontend/src/it-services/compute-vm/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
