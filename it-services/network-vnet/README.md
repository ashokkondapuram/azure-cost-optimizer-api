# Virtual network (`network-vnet`)

IT service folder for **network/vnet**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/network-vnet/manifest.yaml` |
| Backend | `it_services/network_vnet/` |
| Frontend | `frontend/src/it-services/network-vnet/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
