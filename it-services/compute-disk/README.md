# compute-disk

Managed disks IT service. **Drawer UI enabled** — registered in `registry.js`.

| Layer | Path |
|-------|------|
| Manifest | `it-services/compute-disk/manifest.yaml` |
| Backend | `it_services/compute_disk/` |
| Frontend | `frontend/src/it-services/compute-disk/` |

Edit disk analysis, catalog JSON, drawer, and list helpers only in these folders.
