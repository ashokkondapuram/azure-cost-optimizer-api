# AKS cluster (`containers-aks`)

IT service folder for **containers/aks**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/containers-aks/manifest.yaml` |
| Backend | `it_services/containers_aks/` |
| Frontend | `frontend/src/it-services/containers-aks/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
