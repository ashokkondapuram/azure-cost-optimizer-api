# <service-id>

Copy this folder when adding a new IT service.

- Backend: `it_services/<service_id_underscore>/`
- Frontend: `frontend/src/it-services/<service-id>/`
- Register UI in `frontend/src/it-services/registry.js`

See `it-services/compute-disk/` for the managed disks pilot.
