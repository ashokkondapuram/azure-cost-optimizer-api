# Disk snapshot (`compute-snapshot`)

IT service folder for **compute/snapshot**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/compute-snapshot/manifest.yaml` |
| Backend | `it_services/compute_snapshot/` |
| Frontend | `frontend/src/it-services/compute-snapshot/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
