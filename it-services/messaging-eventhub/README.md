# Event Hubs namespace (`messaging-eventhub`)

IT service folder for **messaging/eventhub**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/messaging-eventhub/manifest.yaml` |
| Backend | `it_services/messaging_eventhub/` |
| Frontend | `frontend/src/it-services/messaging-eventhub/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
