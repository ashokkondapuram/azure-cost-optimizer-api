# Service Bus namespace (`messaging-servicebus`)

IT service folder for **messaging/servicebus**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/messaging-servicebus/manifest.yaml` |
| Backend | `it_services/messaging_servicebus/` |
| Frontend | `frontend/src/it-services/messaging-servicebus/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
