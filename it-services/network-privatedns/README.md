# Private DNS zone (`network-privatedns`)

IT service folder for **network/privatedns**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/network-privatedns/manifest.yaml` |
| Backend | `it_services/network_privatedns/` |
| Frontend | `frontend/src/it-services/network-privatedns/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
