# Virtual machine scale set (`compute-vmss`)

IT service folder for **compute/vmss**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/compute-vmss/manifest.yaml` |
| Backend | `it_services/compute_vmss/` |
| Frontend | `frontend/src/it-services/compute-vmss/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
