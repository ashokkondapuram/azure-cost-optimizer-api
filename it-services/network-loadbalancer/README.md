# Load balancer (`network-loadbalancer`)

IT service folder for **network/loadbalancer**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/network-loadbalancer/manifest.yaml` |
| Backend | `it_services/network_loadbalancer/` |
| Frontend | `frontend/src/it-services/network-loadbalancer/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
