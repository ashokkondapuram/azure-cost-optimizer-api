# NAT gateway (`network-nat`)

IT service folder for **network/nat**.

| Layer | Path |
|-------|------|
| Manifest | `it-services/network-nat/manifest.yaml` |
| Backend | `it_services/network_nat/` |
| Frontend | `frontend/src/it-services/network-nat/` |

Edit only files under this service's backend and frontend folders. See `it-services/README.md`.
